{
  "name": "syjpmopmh-pos-web",
  "version": "1.0.0",
  "description": "Web-based POS data entry dashboard",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html *.css lib dist && npm run build:js",
    "build:js": "browserify src/app.js -o dist/app.js",
    "start": "serve dist"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "flatpickr": "^4.6.13",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "serve": "^14.2.3"
  }
}